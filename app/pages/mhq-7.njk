{% extends 'layouts/layout.njk' %}

{% block content %}
  <section class="panel panel--progress">

    {{ section.header(
        title = 'Mental Health Questionnaire' | safe,
        subtitle = 'Assessment' | safe,
        info = '<a href="#">&lt;&nbsp;&nbsp;Back to my assessment</a>' | safe,
        duration = 15,
        step = 1,
        steps = 5
      )
    }}

    <article>
      <h1 class="h2">In your life, have you been a victim of a sexual assault, whether by a stranger or someone you knew?</h1>
      {% call form.form('mhq-form', 'mhq-6.html', 'mhq-8.html', 'Go back', 'Continue') %}
        {{ form.radios('sexual-assault', '', ['No', 'Yes, but not in the last year', 'Yes, during the last year'], required=true) }}
        {{ form.skip_page('mhq-8.html', 'Prefer not to say') }}
      {% endcall %}
      {{ form.skip_section() }}
    </article>

    {{ section.footer( info = '<a id="exit-home-open" class="modal-open" href="#exit-home">Exit assessment and return to home page</a>' | safe ) }}

  </section>
{% endblock %}
