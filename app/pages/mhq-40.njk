{% extends 'layouts/layout.njk' %}

{% block content %}
  <section class="panel panel--progress">

    {{ section.header(
        title = 'Mental Health Questionnaire' | safe,
        subtitle = 'Assessment' | safe,
        info = '<a href="#">Tooltip link</a>' | safe,
        duration = 15,
        step = 1,
        steps = 5
      )
    }}

    <article>
      <h1 class="h2">Is this addiction or dependence ongoing?</h1>
      {% call form.form('mhq-form', 'mhq-39.html', 'mhq-41.html', 'Go back', 'Continue') %}
        {{ form.select_group('gender', '', ['Yes', 'No'], required=true) }}
        {{ form.skip_page('mhq-41.html', 'Prefer not to say') }}
      {% endcall %}
    </article>

    {{ section.footer( info = '<a id="exit-home-open" class="modal-open" href="#exit-home">Exit assessment and return to home page</a>' | safe ) }}

  </section>
{% endblock %}
